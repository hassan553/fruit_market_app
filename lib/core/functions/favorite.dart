import 'package:fruit_market_app/main.dart';

import '../../features/home/model/product_model.dart';

getFavoriteProduct(String id) {}
addFavoriteProduct(ProductModel productModel, String id) async {
  //await sharedPreferences.set(id, productModel.toJson());
}
